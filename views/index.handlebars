{{!-- set up to have the templat that handlebars can renderr onto --}}

{{!-- create a button that will submit the uder inpuut into the database --}}

<h1>Hello world!</h1>

{{!-- class example pasted below --}}

{{!-- <h1>Task Saver</h1>

<h3>Save the tasks you need to get to later</h3>

<ul>
    {{#each tasks}}
        <li>
            <p>ID: {{this.id}}</p>
            <p>Task: {{this.task}}</p>
        </li>
    {{/each}}
</ul>

<form action="/" method="POST">
	<textarea type="text" name="task"></textarea>

	<button type="submit">Submit</button>
</form> --}}